description: STM32 family RNG based on internal temperature sensor

compatible: "st,temp-rng"

include: base.yaml

properties:
    label:
      required: true

    io-channels:
      required: true
      description: ADC channel for temperature sensor

